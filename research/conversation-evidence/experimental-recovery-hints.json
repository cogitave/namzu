{
  "description": "Unshipped recovery-tool-name candidate; patch applies to the availability-only implementation.",
  "patch": "--- a/packages/sdk/src/run/evidence-recall.ts\n+++ b/packages/sdk/src/run/evidence-recall.ts\n@@ -74,6 +74,8 @@\n \treadonly query?: string\n \t/** Resolve references through one run-metered preparation call per operator input. Default false. */\n \treadonly resolveQuery?: boolean\n+\t/** Host-mounted read-only history tool names for failure recovery. At most four; hints only. */\n+\treadonly recoveryTools?: readonly string[]\n }\n \n const HEADER =\n@@ -91,11 +93,19 @@\n function availabilityNote(\n \tstage: 'query_planning' | 'retrieval',\n \treason: 'failed' | 'timeout' | 'pending',\n+\trecoveryTools: readonly string[],\n ): string {\n \treturn `Conversation evidence availability (not retrieved evidence):\\n${JSON.stringify({\n \t\tstatus: 'unavailable',\n \t\tstage,\n \t\treason,\n+\t\t...(recoveryTools.length\n+\t\t\t? {\n+\t\t\t\t\trecoveryTools,\n+\t\t\t\t\trecoveryGuidance:\n+\t\t\t\t\t\t'These read-only history tools remain available despite automatic recall failure. Use their schemas to recover the past observation; the current workspace is not a historical source.',\n+\t\t\t\t}\n+\t\t\t: {}),\n \t\tguidance:\n \t\t\t'This automatic pass supplied no evidence. This does not establish that earlier records are absent. When a past detail is needed, use available read-only conversation-history tools or disclose uncertainty if it remains unverified. Current files do not establish what was observed earlier. Never replay a state-changing action to recover its output.',\n \t})}\\n`\n@@ -106,8 +116,9 @@\n \tstage: 'query_planning' | 'retrieval',\n \treason: 'failed' | 'timeout',\n \tcharBudget: number,\n+\trecoveryTools: readonly string[],\n ): unknown {\n-\tconst note = availabilityNote(stage, reason)\n+\tconst note = availabilityNote(stage, reason, recoveryTools)\n \treturn note.length <= charBudget ? new PreparationContextError(cause, note) : cause\n }\n \n@@ -358,6 +369,16 @@\n \t)\n \t\tthrow new Error('Evidence recall requires a host-bound conversation scope.')\n \tconst retrieve = options.retrieve\n+\tif (\n+\t\toptions.recoveryTools !== undefined &&\n+\t\t(!Array.isArray(options.recoveryTools) ||\n+\t\t\toptions.recoveryTools.length > 4 ||\n+\t\t\toptions.recoveryTools.some(\n+\t\t\t\t(name) => typeof name !== 'string' || !/^[a-zA-Z0-9_.:-]{1,128}$/.test(name),\n+\t\t\t))\n+\t)\n+\t\tthrow new Error('Evidence recall recoveryTools requires at most four valid tool names.')\n+\tconst recoveryTools = [...new Set(options.recoveryTools ?? [])]\n \tconst maxChars = bounded(options.maxChars ?? 6_000, 12_000, 'maxChars')\n \tconst maxPassages = bounded(options.maxPassages ?? 4, 8, 'maxPassages')\n \tconst timeoutMs = bounded(options.timeoutMs ?? 1_000, 10_000, 'timeoutMs')\n@@ -385,7 +406,7 @@\n \t\t\t.slice(0, 16)\n \t\tif (!terms.length || charBudget <= HEADER.length + 200) return undefined\n \t\tif (pending.has(retrieve)) {\n-\t\t\tconst note = availabilityNote('retrieval', 'pending')\n+\t\t\tconst note = availabilityNote('retrieval', 'pending', recoveryTools)\n \t\t\treturn note.length <= charBudget\n \t\t\t\t? { context: [prepared.context, note].filter(Boolean).join('\\n\\n') }\n \t\t\t\t: undefined\n@@ -395,7 +416,7 @@\n \t\t\tresolution = options.resolveQuery ? await resolveQuery(context, query) : undefined\n \t\t} catch (error) {\n \t\t\tsignal?.throwIfAborted()\n-\t\t\tthrow unavailable(error, 'query_planning', 'failed', charBudget)\n+\t\t\tthrow unavailable(error, 'query_planning', 'failed', charBudget, recoveryTools)\n \t\t}\n \t\tsignal?.throwIfAborted()\n \t\tif (resolution === null) return undefined\n@@ -745,6 +766,7 @@\n \t\t\t\t'retrieval',\n \t\t\t\tcontroller.signal.aborted ? 'timeout' : 'failed',\n \t\t\t\tcharBudget,\n+\t\t\t\trecoveryTools,\n \t\t\t)\n \t\t} finally {\n \t\t\tclearTimeout(timer)\n--- a/packages/cli/src/integrations/sessions/evidence-recall.ts\n+++ b/packages/cli/src/integrations/sessions/evidence-recall.ts\n@@ -59,6 +59,7 @@\n \treturn createEvidenceRecallStep({\n \t\tscope,\n \t\tresolveQuery,\n+\t\trecoveryTools: CONVERSATION_RETRIEVAL_TOOLS,\n \t\tasync retrieve({ runId, terms, signal, maxReadBytes, maxCandidates, captureRunEvidence }) {\n \t\t\tassertOwner(runId)\n \t\t\tconst candidates: EvidenceRecallCandidate[] = []\n"
}
