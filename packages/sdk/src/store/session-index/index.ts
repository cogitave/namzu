// The rebuildable index over every session log (`SqliteSessionIndex` where
// `node:sqlite` loads, `ScanSessionIndex` otherwise). Filled by the
// SessionIndex workstream; the public barrels already re-export this module.
export {}
