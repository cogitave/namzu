{
	"$comment": "Coverage gate config for @namzu/sdk. Consumed by .github/scripts/check-sdk-module-coverage.mjs and check-sdk-test-presence.mjs. The bucket rules were ratified in ses_006-test-coverage-sprint.",
	"$comment_pricing": "`pricing` is `required` with a measured floor, and the floor only means anything because its generated rate table is excluded from coverage in vitest.config.ts. Measured with the table IN: 299 lines, 282 of them the generated data, all counted covered the moment anything imports the module — so the aggregate read 100% while describing almost nothing, and the resolver could have lost a third of its lines and still cleared 97%. Measured with it OUT: 17 lines and 10 branches, all in index.ts, 100%/100%. Floor is measured minus 3 per the policy in check-sdk-module-coverage.mjs. Note the module is small enough that 3 points is less than one branch, so any uncovered branch in the resolver fails this gate — that is intended, not an accident of rounding.",
	"modules": {
		"required": [
			"__fixtures__",
			"advisory",
			"agents",
			"authorization",
			"bridge",
			"bus",
			"compaction",
			"config",
			"connector",
			"directory",
			"eval",
			"execution",
			"invariants",
			"manager",
			"plugin",
			"pricing",
			"probe",
			"process",
			"prompt",
			"provider",
			"rag",
			"read-model",
			"registry",
			"run",
			"run-query",
			"runtime",
			"sandbox",
			"scheduler",
			"session",
			"skills",
			"store",
			"streaming",
			"telemetry",
			"tools",
			"utils",
			"vault"
		],
		"baselineExempt": [
			"model-router",
			"persona"
		],
		"exempt": [
			"__tests__",
			"constants",
			"contracts",
			"test-support",
			"types"
		]
	},
	"coverageFloors": {
		"advisory": {
			"line": 94,
			"branch": 91
		},
		"bridge": {
			"line": 96,
			"branch": 95
		},
		"bus": {
			"line": 94,
			"branch": 91
		},
		"connector": {
			"line": 47,
			"branch": 84
		},
		"directory": {
			"line": 85,
			"branch": 82
		},
		"pricing": {
			"line": 97,
			"branch": 97
		},
		"rag": {
			"line": 92,
			"branch": 92
		},
		"registry": {
			"line": 93,
			"branch": 90
		}
	},
	"$comment_baseline_exempt": "`baselineExempt` means ZERO-TESTED, per check-sdk-test-presence.mjs's own docblock. It had drifted badly: seven of its nine entries carried tests -- `utils` had 25 files -- so the list said 'these have no tests' about modules that did. A routing document that is false is worse than none, because the next person picks the wrong module to work on. Audited and corrected in ses_020; `model-router` and `persona` are the two that genuinely have none. Moving a module here into `required` asserts only that it HAS a test, which is what this gate checks -- coverage is the separate, opt-in `coverageFloors` claim, and none of the seven carries one yet."
}
