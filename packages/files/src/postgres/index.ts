// Postgres BlobStore (file_blobs bytea) + FileRegistry over the
// canonical Vandal schema (files / file_links / file_text_documents
// / file_scope_revisions). Implementation lands in Phase 1.
export {}
