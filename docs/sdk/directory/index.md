# Directory

An agent as a directory on disk.

* [An agent can be a directory](agent-as-a-directory.md) - The conventional folder the SDK reads into run options — its five slots, the import-nothing load mode, the diagnostics it returns instead of dropping work, and the two ways a folder-defined agent can be given its system prompt.
