---
"@namzu/evals": patch
---

Use SDK-generated UUIDs and the current `topicId` field in the shipped kernel eval suites so they run against the SDK's UUID-only identity contract. Eval cases and scoring behavior are unchanged.
