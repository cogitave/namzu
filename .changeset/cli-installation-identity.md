---
"@namzu/cli": patch
---

Show the CLI version, absolute entrypoint and executable-file fingerprint in /status so stale installations can be distinguished from a newer local build with the same package version.
