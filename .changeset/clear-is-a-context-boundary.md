---
'@namzu/cli': major
---

Change `/clear` to start a new resumable conversation context as well as clearing the terminal. Use `/clear-screen` to retain the previous screen-only behavior. Add `/new` to start the same fresh context without clearing visible scrollback.
