---
'@namzu/cli': minor
---

Turn `/help` into an interactive, height-aware command palette that includes the live kernel and project command vocabulary and dispatches the selected row through the ordinary slash-command path.
