---
'@namzu/cli': patch
---

Present parallel delegated work as named tasks instead of a raw internal tool envelope. After approval, the TUI now returns immediately to a visible Working state, shows the active child count, and lets Ctrl+T open the delegated-work cockpit.
