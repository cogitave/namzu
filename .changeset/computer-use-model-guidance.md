---
"@namzu/sdk": patch
---

`computer_use` clicks and drags with the left button when a call leaves `button` out, instead of refusing the call (a model asked to click the Start button sent no button and spent a round trip on the refusal). Its description now tells the model to batch the steps it can predict and, on Windows, to start programs through the Run dialog rather than Start-menu search, which on a non-English Windows turned an English program name into a web search in the browser. With a host that reads controls, the first screenshot says so. No change is needed on your side.
