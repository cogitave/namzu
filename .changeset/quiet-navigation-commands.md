---
'@namzu/cli': minor
---

Add selectable `/pwd` and `/mention` commands. `/pwd` reports the active
session directory, while `/mention` restores an editable `@` file token in the
composer without starting a model turn.
