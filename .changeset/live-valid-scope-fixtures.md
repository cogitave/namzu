---
"@namzu/live": patch
---

Validate the Namzu live adapter with factory-generated project, session, topic and tenant IDs. Its real-query regression fixtures now satisfy the SDK's durable path validation without bypassing the query configuration types.
