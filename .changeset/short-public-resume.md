---
"@namzu/cli": patch
---

Show `namzu resume <id>` on exit when the executable on PATH matches the running CLI. Preserve explicit executable paths for alternate installations and source launches, and include a directory change only when the conversation uses a different working directory.
