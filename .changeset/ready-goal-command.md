---
'@namzu/cli': patch
---

Release the durable goal-command input gate before showing its completed result, so an operator can immediately follow a visible goal status with `/goal resume` without a false in-flight refusal.
