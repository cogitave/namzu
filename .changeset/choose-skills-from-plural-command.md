---
'@namzu/cli': minor
---

Open bare `/skills` as the discovered skill chooser. Preserve the previous text
roster as `/skills list`, allow `/skills <name>` for direct activation, and keep
`/skill` as a compatible alias.
