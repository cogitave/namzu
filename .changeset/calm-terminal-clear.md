---
'@namzu/cli': minor
---

Add Ctrl+L as an idle-only terminal display clear. It preserves model and
durable conversation history like `/clear-screen`, and refuses while a turn is
still producing output.
