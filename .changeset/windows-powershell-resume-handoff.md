---
"@namzu/cli": patch
---

Make the Windows conversation resume hint executable in PowerShell, including Windows PowerShell 5.1. The hint explicitly names PowerShell, quotes paths and arguments literally, and resumes only after a required directory change succeeds.
