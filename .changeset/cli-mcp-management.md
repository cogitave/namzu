---
"@namzu/cli": minor
---

Add `namzu mcp list|get|add|remove` for user-owned tool servers. Existing server config continues to work; these commands write `~/.namzu/config.yaml`, preserve other settings, and hide credential-bearing values in their output.
