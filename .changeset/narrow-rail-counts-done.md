---
"@namzu/cli": patch
---

On a terminal under 64 columns the agent rail's header now counts agents done out of the total (`2/3 done`), the figure its wide form and the agent cockpit's header give at the same moment. It used to print the agents still running over the total with no word after it (`1/3`), which read as one agent done. Nothing to change on your side.
