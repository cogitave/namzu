---
"@namzu/sdk": patch
---

Replace the SDK quick start that failed at runtime because it omitted required session identity with complete, offline `runAgent` examples. The examples show a real tool execution, the supported Zod peer range, and how identity and history are carried between turns.
